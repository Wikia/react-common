// This file is generated automatically via generate-icon-components.js
import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

import IconDropdown2Svg from '../../_icons/24px/dropdown2.svg';

const IconDropdown2 = styled(({ size, ...rest }) => <IconDropdown2Svg height={size} width={size} {...rest} />)`
    fill: currentColor;
    height: ${props => props.size}px;
    width: ${props => props.size}px;
`;

IconDropdown2.propTypes = {
    /** Size of the icon */
    size: PropTypes.number,
};

IconDropdown2.defaultProps = {
    size: 24,
};

// @component
export default IconDropdown2;
