// This file is generated automatically via generate-icon-components.js
import styled from 'styled-components';
import PropTypes from 'prop-types';

import IconMagnifyingGlassSvg from '../../_icons/24x24/magnifying-glass.svg';

const IconMagnifyingGlass = styled(IconMagnifyingGlassSvg)`
    fill: currentColor;
    height: ${props => props.size}px;
    width: ${props => props.size}px;
`;

IconMagnifyingGlass.propTypes = {
    size: PropTypes.number,
};

IconMagnifyingGlass.defaultProps = {
    size: 24,
};

// @component
export default IconMagnifyingGlass;
