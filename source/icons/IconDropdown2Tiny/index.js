// This file is generated automatically via generate-icon-components.js
import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

import IconDropdown2TinySvg from '../../_icons/12px/dropdown2.svg';

const IconDropdown2Tiny = styled(({ size, ...rest }) => <IconDropdown2TinySvg height={size} width={size} {...rest} />)`
    fill: currentColor;
    height: ${props => props.size}px;
    width: ${props => props.size}px;
`;

IconDropdown2Tiny.propTypes = {
    /** Size of the icon */
    size: PropTypes.number,
};

IconDropdown2Tiny.defaultProps = {
    size: 12,
};

// @component
export default IconDropdown2Tiny;
