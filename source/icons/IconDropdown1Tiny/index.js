// This file is generated automatically via generate-icon-components.js
import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

import IconDropdown1TinySvg from '../../_icons/12px/dropdown1.svg';

const IconDropdown1Tiny = styled(({ size, ...rest }) => <IconDropdown1TinySvg height={size} width={size} {...rest} />)`
    fill: currentColor;
    height: ${props => props.size}px;
    width: ${props => props.size}px;
`;

IconDropdown1Tiny.propTypes = {
    /** Size of the icon */
    size: PropTypes.number,
};

IconDropdown1Tiny.defaultProps = {
    size: 12,
};

// @component
export default IconDropdown1Tiny;
