{
  "name": "react-design-system",
  "version": "0.0.7",
  "description": "Reusable React components based on Wikia's Design System",
  "main": "index.js",
  "repository": "git@github.com:Wikia/react-design-system.git",
  "license": "PRIVATE",
  "private": true,
  "dependencies": {
    "prop-types": "^15.6.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "build": "styleguidist build",
    "lint:fix": "stylelint \"components/*.css\" --fix",
    "lint:css": "stylelint \"components/*.css\"",
    "lint:js": "eslint .",
    "lint": "yarn lint.js && yarn lint.css",
    "release": "yarn release:patch",
    "release:major": "git checkout master && git pull && yarn build && yarn version --new-version major && git push --follow-tags",
    "release:minor": "git checkout master && git pull && yarn build && yarn version --new-version minor && git push --follow-tags",
    "release:patch": "git checkout master && git pull && yarn build && yarn version --new-version patch && git push --follow-tags",
    "test": "yarn lint",
    "watch": "styleguidist server"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "eslint": "^4.3.0",
    "eslint-config-fandom": "git://github.com/Wikia/eslint-config-fandom.git#2.0.0",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-jsx-a11y": "^5.1.1",
    "eslint-plugin-react": "^7.1.0",
    "react-styleguidist": "^6.2.6",
    "stylelint": "^8.1.1",
    "stylelint-config-fandom": "git://github.com/Wikia/stylelint-config-fandom.git#0.0.1",
    "stylelint-order": "^0.7.0",
    "stylelint-scss": "^2.1.0",
    "webpack": "3"
  }
}
